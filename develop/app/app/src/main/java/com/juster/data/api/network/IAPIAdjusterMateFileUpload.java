package com.juster.data.api.network;

import com.squareup.okhttp.RequestBody;
import retrofit.http.Body;
import retrofit.http.Header;
import retrofit.http.POST;

/**
 * Created by Anurag Singh on 14/12/15 2:42 PM.
 */
public interface IAPIAdjusterMateFileUpload {

    /*@POST("/")
    Call<String> upload(
            @Header(IAPIConstants.HEADER_DEVICE_ID) String deviceId,
            @Header(IAPIConstants.HEADER_STOKEN) String sToken,
            @Header("filename") String fileName,
            @Header("mimeType") String mimeType,
            @Part("attachment") RequestBody file
    );*/

    /*@POST("/")
    Call<ArrayList<KeepUploadFileResponse>> upload(@Header(IAPIConstants.HEADER_DEVICE_ID) String
                                                           deviceId, @Header(IAPIConstants
            .HEADER_STOKEN) String sToken, @Header("filename") String fileName, @Header
            ("mimeType") String mimeType, @Body RequestBody photo);*/

}
